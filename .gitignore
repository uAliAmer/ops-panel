node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
!.env.demo

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Only the open-licensed faces that ship with this repository belong in here.
# Any other font binary is someone else's to license, so it stays out.
src/lib/fonts/*.woff2
src/lib/fonts/*.woff
src/lib/fonts/*.otf
.gstack/
